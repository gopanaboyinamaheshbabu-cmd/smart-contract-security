# 2026-08-13

## What did I learn today?

- Learned about **Custom Errors** in Solidity.
- Learned how custom errors can reduce gas usage compared to `require` with error strings.
- Learned about the **`receive()`** function.
- Learned about the **`fallback()`** function.
- Understood the difference between `receive()` and `fallback()`.
- Learned what **call data** is and how an unknown call can trigger the `fallback()` function.

## What did I practice today?

- Practiced understanding how `receive()` handles direct ETH transfers.
- Practiced understanding how `fallback()` handles unknown calls and calldata.
- Solved **one level of Ethernaut** and understood the basic working of the challenge.

## What did I break today?

- Nothing specifically.

## What vulnerability did I understand today?

- Understood how unexpected or unknown calls can reach a contract's `fallback()` function.
- Practiced thinking about contract behavior from a security perspective through Ethernaut.

## What production code did I read today?

- Read and understood the basics of **ERC-20 tokens**.

## What pattern was added to my mental library?

- Custom errors → cheaper error handling.
- `receive()` → handles plain ETH transfers.
- `fallback()` → handles unknown calls.
- Call data → instructions sent to a contract.
- Ethernaut → practice understanding real smart-contract vulnerabilities.
- ERC-20 → standard interface for fungible tokens.