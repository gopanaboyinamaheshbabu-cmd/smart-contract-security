# 2026-08-19

## What did I learn today?

* Learned about **mock contracts** and why they are useful for local smart contract testing.
* Learned how `HelperConfig.s.sol` manages different network configurations.
* Learned how `block.chainid` helps select the correct configuration for the current network.
* Understood how a **MockV3Aggregator** can simulate the Chainlink ETH/USD price feed on Anvil.
* Learned how to make tests **network agnostic**, so they can run locally without always using a Sepolia fork.
* Understood how to avoid deploying the same mock price feed multiple times using an `address(0)` check.

## What did I practice today?

* Created and worked with `HelperConfig.s.sol`.
* Configured the Sepolia price feed address.
* Worked on creating a mock price feed for Anvil.
* Connected the selected price feed configuration to the `FundMe` deployment.
* Ran `forge test` and debugged the price feed error on the local Anvil environment.

## What did I break today?

* Initially passed `address(0)` to `FundMe`, which caused `testVersion()` to fail with:
  `call to non-contract address`.
* Fixed the issue by using `HelperConfig` to provide the correct price feed address.

## What vulnerability did I understand today?

* No specific vulnerability was studied today.
* Focus was mainly on **testing, mocking, configuration management, and network-independent deployment**.

## What production code did I read today?

* None.

## What pattern was added to my mental library?

* **Real network → use the real Chainlink contract.**
* **Local Anvil → use a mock contract.**
* `HelperConfig` acts as the bridge between the deployment code and network-specific configuration.
* `block.chainid` can be used to automatically choose the correct configuration.
* Mock contracts allow smart contracts that depend on external contracts to be tested locally.
